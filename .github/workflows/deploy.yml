name: Deploy Bot

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: git -C ~/discordBot pull

      - name: Restart bot
        run: sudo systemctl restart discordbot
